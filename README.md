The project is a Blog API using the full set of Django REST Frameworkfeatures. It has users, permissions, and allow for full CRUD (Create-Read-Update-Delete) functionality.
and also documentation isprepared.
